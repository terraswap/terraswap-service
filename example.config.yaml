api:
  name: terraswap-service
  host: 0.0.0.0
  port: 8000
  swagger: false
  mod: release # test, debug, release
  env:

sentry:
  # "https://putYourSentry@sentry.io/Id"
  dsn:

log:
  # panic, fatal, error, warn, info, debug, trace
  level: info
  formatJson: true
  # prod, dev
  env:

terraswap:
  chainId: pisco-1
  version: # Optional v1 or v2 for columbus
  grpcHost:
  insecureConnection: true
  cw20AllowlistUrl: https://raw.githubusercontent.com/terra-money/assets/master/cw20/tokens.js
  ibcAllowlistUrl: https://raw.githubusercontent.com/terra-money/assets/master/ibc/tokens.js
  filterUnverified: false

rdb:
  host:
  port:
  database:
  username:
  password:

cache:
  host:
  db:
  tlsRequired:
  ca:
  authRequired:
  password:
